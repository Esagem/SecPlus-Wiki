---
---

# Wiki Activity Log

> **STARTER FILE.** Copy this to `_log.md` at the wiki root. The header below is the convention every wiki uses; the body below it will accumulate on its own. Delete this "STARTER FILE" callout once placed.

Append-only. Every mutating MCP tool call appends an entry here in the format:

```
## [YYYY-MM-DD] <op> | <context> | <short description>
```

Operations: `ingest`, `session`, `quiz`, `lint`, `schema`, `write`, `edit`, `status`, `delete`.

This file is the chronological source of truth for what changed and when. Use `wiki_log_tail` instead of reading it directly for routine queries.

---
