---
title: 4.6 — Identity and Access Management Quiz
category: quiz
tags: [quiz, iam, authentication, mfa, access-control, 4.6]
status: active
confidence: high
created: 2026-05-21
updated: 2026-05-21
covers: [4.6]
---

# 4.6 — Identity and Access Management Quiz

Twelve questions on IAM lifecycle, federation, access-control models, MFA, and password security from [[objectives/4.6|Objective 4.6]].

```dataviewjs
const questions = [
  {
    q: "A new hire's account is created on day one with email, shared-drive, and HR-app permissions, and nothing else. Which IAM principle BEST describes this?",
    opts: [
      { t: "Mandatory access control", c: false, w: "MAC uses clearance labels assigned by the administrator. The scenario describes scope, not labels." },
      { t: "Least privilege", c: true, w: "Correct. Grant exactly the rights needed to do the job — no admin rights, no extra app access." },
      { t: "Just-in-time permissions", c: false, w: "JIT issues temporary admin credentials on request. This is permanent baseline access at onboarding." },
      { t: "Federation", c: false, w: "Federation is using an external identity provider — unrelated to scope of permissions." },
    ],
  },
  {
    q: "Before creating an account for a new employee, the security team requires the candidate to present a government-issued ID in person. Which step of identity proofing is this?",
    opts: [
      { t: "Resolution", c: false, w: "Resolution uniquely identifies the candidate (name, employee ID). This is the evidence step that follows." },
      { t: "Validation", c: false, w: "Validation captures what the user knows or possesses (password, security questions). ID verification is the final step." },
      { t: "Attestation", c: true, w: "Correct. Attestation verifies the claimed identity using authoritative evidence — government ID, in-person meeting, KBA from credit history." },
      { t: "Authorization", c: false, w: "Authorization happens after the identity exists and tries to access a resource. Proofing comes first." },
    ],
  },
  {
    q: "A user opens a corporate web app in their browser. The app redirects them to an identity server, where they authenticate; the server returns a signed XML assertion that the app accepts. Which protocol is this?",
    opts: [
      { t: "OAuth", c: false, w: "OAuth is an authorization framework that issues bearer tokens, not signed XML assertions. It also typically pairs with OpenID for authentication." },
      { t: "SAML", c: true, w: "Correct. Three-party browser SSO with a signed XML assertion is the textbook SAML flow." },
      { t: "LDAP", c: false, w: "LDAP is a directory protocol for reading user/group records — not a browser-redirect SSO flow." },
      { t: "Kerberos", c: false, w: "Kerberos uses tickets within a realm (commonly inside AD), not browser-based assertions across organizations." },
    ],
  },
  {
    q: "You authorize a mobile app to read and write files in your cloud storage account. The cloud provider authenticates you, then issues the app a scoped token. Which protocol grants the app this delegated access?",
    opts: [
      { t: "SAML", c: false, w: "SAML is designed for browser SSO between enterprises, not for mobile-app delegated authorization." },
      { t: "OAuth", c: true, w: "Correct. OAuth is the authorization framework that lets a third-party app act on a user's behalf with a scoped token. OpenID Connect on top handles authentication." },
      { t: "RADIUS", c: false, w: "RADIUS is a backend AAA protocol for network access (VPN, 802.1X), not third-party app authorization." },
      { t: "X.500", c: false, w: "X.500 is the directory specification that LDAP implements, not an authorization framework." },
    ],
  },
  {
    q: "What's the KEY difference between MAC and DAC?",
    opts: [
      { t: "MAC uses roles; DAC uses labels", c: false, w: "Backwards on both. MAC uses labels (Confidential, Secret); RBAC uses roles. DAC has neither — the owner decides." },
      { t: "In MAC the administrator assigns access via labels; in DAC the data owner decides who else gets access", c: true, w: "Correct. MAC = mandate from admin via clearance labels. DAC = owner's discretion over their own resources." },
      { t: "MAC works only on Windows; DAC works only on Linux", c: false, w: "Both are platform-agnostic concepts. SELinux is one MAC implementation on Linux; Windows defaults to DAC for user files." },
      { t: "MAC requires MFA; DAC does not", c: false, w: "Neither model dictates authentication factors — they describe authorization after authentication." },
    ],
  },
  {
    q: "An administrator configures a rule: 'This dataset is readable only between 9am and 5pm.' Which access-control model is this MOST consistent with?",
    opts: [
      { t: "DAC", c: false, w: "DAC lets the data owner decide access — the rule here is set by the administrator, not the owner." },
      { t: "RBAC", c: false, w: "RBAC ties permissions to roles. This rule is attached to the object itself, not to a role." },
      { t: "Rule-based access control", c: true, w: "Correct. Rules attached to objects and enforced at access time — time-of-day windows are the canonical example." },
      { t: "Federation", c: false, w: "Federation is an identity-source pattern, not an access-control model." },
    ],
  },
  {
    q: "A policy reads: 'Allow write access to financial records only if the requester's role is Finance, the device is corporate-managed, the time is between 8am and 6pm, and the source IP is in the US.' Which model BEST supports this?",
    opts: [
      { t: "MAC", c: false, w: "MAC is single-axis clearance labels, not multi-attribute composites." },
      { t: "DAC", c: false, w: "DAC delegates access to data owners; no policy engine here would combine four attributes." },
      { t: "RBAC", c: false, w: "RBAC keys off role alone. The scenario combines role, device, time, and location." },
      { t: "ABAC", c: true, w: "Correct. ABAC composes multiple attributes — user, device, time, location, action — into a single policy decision." },
    ],
  },
  {
    q: "Which of these combinations IS true multifactor authentication?",
    opts: [
      { t: "Password and security questions", c: false, w: "Both are 'something you know' — single factor in two forms." },
      { t: "Password and a code from a smartphone authenticator app", c: true, w: "Correct. Something you know (password) + something you have (the phone with the TOTP app)." },
      { t: "Two different passwords", c: false, w: "Same category twice — not MFA." },
      { t: "Username and password", c: false, w: "Username is an identifier, not an authentication factor. Password alone is single-factor." },
    ],
  },
  {
    q: "Which statement about biometric authentication is TRUE?",
    opts: [
      { t: "The system stores a photograph of the fingerprint or a recording of the voice", c: false, w: "Systems store a mathematical TEMPLATE derived from the biometric, not the raw image or audio." },
      { t: "If a biometric is compromised, the user can easily issue themselves a new fingerprint", c: false, w: "You can't change your fingerprint — that's exactly why biometrics are usually paired with another factor." },
      { t: "Biometrics are stored as a mathematical representation and are typically paired with another factor", c: true, w: "Correct. Templates, not raw data; and pairing compensates for the fact that biometrics can be circumvented and can't be revoked." },
      { t: "Biometrics are a 'something you have' factor", c: false, w: "Biometrics are 'something you ARE'." },
    ],
  },
  {
    q: "A mobile-app login is rejected because the user appears to be in a country 6,000 miles from where they were 10 minutes ago. Which authentication factor triggered the rejection?",
    opts: [
      { t: "Something you know", c: false, w: "That's password / PIN / pattern — unrelated to location." },
      { t: "Something you have", c: false, w: "That's smart cards, tokens, USB keys — not the user's geographic position." },
      { t: "Something you are", c: false, w: "Biometric — fingerprint, face, voice. Not location-based." },
      { t: "Somewhere you are", c: true, w: "Correct. Geolocation (GPS + IP) is the 'somewhere you are' factor. Impossible-travel detection is a common use." },
    ],
  },
  {
    q: "An IT department wants technicians to perform admin-level fixes WITHOUT giving them standing admin accounts. Which approach BEST fits?",
    opts: [
      { t: "Give every technician permanent admin rights", c: false, w: "Permanent admin rights are the problem this is meant to solve — large blast radius if compromised." },
      { t: "Just-in-time (JIT) permissions from a central password vault", c: true, w: "Correct. The vault holds primary credentials and issues short-lived, scoped ephemeral credentials on request; rights time out after the session." },
      { t: "A shared admin account stored in a wiki page", c: false, w: "Shared credentials in plaintext destroy accountability and auditability." },
      { t: "MFA on the user's normal account only", c: false, w: "MFA hardens authentication but doesn't address standing admin rights." },
    ],
  },
  {
    q: "Which BEST describes the relationship between federation and protocols like SAML or OAuth/OpenID?",
    opts: [
      { t: "Federation is a protocol that competes with SAML and OAuth", c: false, w: "Federation is not itself a wire protocol — it's an architectural pattern." },
      { t: "Federation is an architectural choice to accept identities from a partner IdP; SAML and OAuth/OpenID are protocols that implement it", c: true, w: "Correct. Federation says 'trust this IdP for identity.' SAML, OAuth + OpenID Connect are how that trust is expressed on the wire." },
      { t: "Federation is only possible with on-prem LDAP", c: false, w: "Federation is most commonly cloud-driven — Google, Microsoft, Facebook, Okta as IdPs." },
      { t: "Federation requires every user to also have a local account", c: false, w: "The whole point of federation is to avoid creating a local account — authenticate against the partner IdP instead." },
    ],
  },
];

// ---- Render ----
const root = dv.container;
root.empty();

const style = root.createEl("style");
style.textContent = `
  .quiz-card { border: 1px solid var(--background-modifier-border); padding: 18px 22px; margin-bottom: 22px; border-radius: 8px; background: var(--background-primary-alt); }
  .quiz-q { font-weight: 600; margin-bottom: 14px; line-height: 1.5; font-size: 1.0em; }
  .quiz-opt { display: flex; align-items: center; width: 100%; text-align: left; padding: 11px 14px; margin: 8px 0; border: 1px solid var(--background-modifier-border); background: var(--background-secondary); cursor: pointer; border-radius: 6px; color: var(--text-normal); font-size: 0.95em; line-height: 1.45; transition: all 120ms ease; }
  .quiz-opt:hover:not(:disabled) { background: var(--background-modifier-hover); }
  .quiz-opt.correct { background: rgba(63, 185, 80, 0.13); border-color: rgba(63, 185, 80, 0.45); color: var(--text-normal); }
  .quiz-opt.incorrect { background: rgba(248, 81, 73, 0.10); border-color: rgba(248, 81, 73, 0.35); color: var(--text-muted); opacity: 0.78; cursor: not-allowed; }
  .quiz-feedback { margin-top: 14px; padding: 11px 14px; border-radius: 6px; font-size: 0.9em; line-height: 1.5; }
  .quiz-feedback.correct { background: rgba(63, 185, 80, 0.06); border-left: 3px solid rgba(63, 185, 80, 0.6); color: var(--text-normal); }
  .quiz-feedback.incorrect { background: rgba(248, 81, 73, 0.06); border-left: 3px solid rgba(248, 81, 73, 0.55); color: var(--text-normal); }
  .quiz-progress { margin-bottom: 20px; padding: 10px 14px; background: var(--background-secondary); border-radius: 6px; font-size: 0.9em; color: var(--text-muted); }
`;

const progress = root.createDiv({ cls: "quiz-progress" });
let solvedCount = 0;
let wrongCount = 0;
const updateProgress = () => {
  progress.setText(`Progress: ${solvedCount} / ${questions.length} solved · ${wrongCount} wrong attempts`);
};
updateProgress();

questions.forEach((qData, i) => {
  const card = root.createDiv({ cls: "quiz-card" });
  card.createEl("div", { cls: "quiz-q", text: `Q${i + 1}. ${qData.q}` });
  let solved = false;
  const feedbackEl = card.createDiv({ cls: "quiz-feedback", attr: { style: "display:none" } });

  qData.opts.forEach((opt) => {
    const btn = card.createEl("button", { cls: "quiz-opt", text: opt.t });
    btn.onclick = () => {
      if (solved) return;
      if (opt.c) {
        btn.classList.add("correct");
        feedbackEl.style.display = "";
        feedbackEl.className = "quiz-feedback correct";
        feedbackEl.setText("✓ " + opt.w);
        solved = true;
        solvedCount++;
        updateProgress();
      } else {
        btn.classList.add("incorrect");
        btn.disabled = true;
        feedbackEl.style.display = "";
        feedbackEl.className = "quiz-feedback incorrect";
        feedbackEl.setText("✗ " + opt.w);
        wrongCount++;
        updateProgress();
      }
    };
  });
});
```

---

## Misses log

- *(none yet)*
