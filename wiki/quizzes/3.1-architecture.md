---
title: 3.1 — Architecture & Infrastructure Models Quiz
category: quiz
tags: [quiz, architecture, infrastructure, 3.1]
status: active
confidence: high
created: 2026-05-21
updated: 2026-05-21
covers: [3.1]
---

# 3.1 — Architecture & Infrastructure Models Quiz

Thirteen questions on architecture models, isolation, virtualization vs containers, IoT/SCADA/embedded, HA, and the cross-cutting "-ilities" from [[objectives/3.1|Objective 3.1]].

```dataviewjs
const questions = [
  {
    q: "In a PaaS deployment, who is responsible for patching the operating system?",
    opts: [
      { t: "The customer", c: false, w: "Customer owns the OS in IaaS and on-prem. In PaaS, the OS is abstracted away." },
      { t: "The cloud provider", c: true, w: "Correct. In PaaS the provider manages the OS and runtime; the customer handles data, app code, and identities." },
      { t: "A third-party vendor", c: false, w: "Third parties manage their own products, not the platform itself." },
      { t: "It's a shared responsibility split 50/50", c: false, w: "The responsibility matrix is layer-by-layer, not a percentage split. OS in PaaS lands fully on the provider." },
    ],
  },
  {
    q: "Regardless of whether you're using IaaS, PaaS, or SaaS, who is ALWAYS responsible for accounts and identities?",
    opts: [
      { t: "The cloud provider", c: false, w: "Identity is the one layer that never crosses to the provider — they don't know which of your employees should have access." },
      { t: "The customer", c: true, w: "Correct. Account and identity management is the customer's job in every cloud service model." },
      { t: "Depends on contract", c: false, w: "Contracts can adjust other rows of the matrix; identity ownership is the constant." },
      { t: "Whoever wrote the application", c: false, w: "The app developer doesn't own identity for the organization deploying the app." },
    ],
  },
  {
    q: "Your team describes the entire web app stack — hosts, web servers, database, firewalls — in a Terraform file that can rebuild the environment on demand. What is this practice called?",
    opts: [
      { t: "Microservices architecture", c: false, w: "Microservices is how an app is broken into services; IaC is how infrastructure is described as code." },
      { t: "Serverless architecture", c: false, w: "Serverless removes server instances from the application's concerns; it's not about describing infrastructure." },
      { t: "Infrastructure as code", c: true, w: "Correct. The infrastructure itself is defined in code, version-controlled, and applied to rebuild identical environments." },
      { t: "Software-defined networking", c: false, w: "SDN is specifically about pulling apart switching/routing planes; IaC describes the whole infrastructure." },
    ],
  },
  {
    q: "A client makes a request and an API gateway routes it to one of many small, independently scalable services. Which architecture is this?",
    opts: [
      { t: "Monolithic", c: false, w: "A monolith has no API gateway in front of small services — it's one big executable." },
      { t: "Microservices", c: true, w: "Correct. API gateway in front of many small services that scale and fail independently — textbook microservices." },
      { t: "Serverless", c: false, w: "Serverless might use functions, but the defining feature here is the API-gateway-fronted service decomposition." },
      { t: "Containerized monolith", c: false, w: "Containerizing a monolith doesn't turn it into microservices — it's still one app, just packaged differently." },
    ],
  },
  {
    q: "A managed service provider physically separates each customer onto its own switch with no cable between switches. What kind of isolation is this?",
    opts: [
      { t: "VLAN isolation", c: false, w: "VLANs separate logically on the same switch. The question says separate physical switches with no link." },
      { t: "Air gap", c: true, w: "Correct. Physically separated, no connectivity between switches — that is an air gap." },
      { t: "Software-defined segmentation", c: false, w: "SDN can implement segmentation, but the scenario describes physical separation, not software." },
      { t: "Zero Trust microsegmentation", c: false, w: "Microsegmentation is a logical policy approach. Air gap is the physical equivalent." },
    ],
  },
  {
    q: "An admin SSHes into a router and modifies an access control list. Which plane of SDN is being used?",
    opts: [
      { t: "Data plane", c: false, w: "Data plane moves packets. Configuration doesn't move packets — it changes how they'll be moved later." },
      { t: "Control plane", c: false, w: "Control plane decides next-hop and routing decisions. Configuring those decisions happens one plane up." },
      { t: "Management plane", c: true, w: "Correct. SSH, SNMP, API config calls — all management plane. Configuration changes always live here." },
      { t: "Forwarding plane", c: false, w: "'Forwarding plane' is another name for the data plane — that's packet movement, not configuration." },
    ],
  },
  {
    q: "A virtualized host runs three guest VMs, all running the same Linux distro. What efficiency does containerization offer over this setup?",
    opts: [
      { t: "Containers eliminate the need for an API gateway", c: false, w: "API gateways are for microservices, not for VM-vs-container efficiency." },
      { t: "Containers share a single host OS rather than running three separate guest OS copies", c: true, w: "Correct. The hypervisor runs three full guest OSes; containers share the host kernel, removing the duplication." },
      { t: "Containers can run multiple operating systems on one host kernel", c: false, w: "Backwards — containers must match the host OS family. VMs are what gives you mixed OSes." },
      { t: "Containers provide stronger kernel isolation than VMs", c: false, w: "VMs isolate at the hypervisor layer (stronger). Containers share the kernel — lighter but a kernel exploit crosses containers." },
    ],
  },
  {
    q: "Which operating system type is REQUIRED for anti-lock brakes, where one critical task must instantly preempt everything else?",
    opts: [
      { t: "A general-purpose desktop OS such as Linux", c: false, w: "Desktop OSes are non-deterministic — no guarantee a task can grab all resources at the right millisecond." },
      { t: "A real-time operating system (RTOS)", c: true, w: "Correct. RTOSes are deterministic — a critical task can preempt everything and respond within strict timing bounds." },
      { t: "An embedded Linux build with low-latency patches", c: false, w: "Low-latency tuning helps, but for safety-critical real-time guarantees the answer the exam wants is RTOS." },
      { t: "A hypervisor with dedicated CPU pinning", c: false, w: "CPU pinning improves performance in VMs but doesn't make a non-deterministic OS deterministic." },
    ],
  },
  {
    q: "A purpose-built HVAC controller has no patch from the vendor and no way to install one. What is the BEST mitigation?",
    opts: [
      { t: "Patch it using a custom firmware build", c: false, w: "Custom firmware on embedded systems is risky and rarely supported. Not the exam's expected answer." },
      { t: "Disconnect it permanently from the network", c: false, w: "Sometimes acceptable, but the device usually needs network connectivity to function. The exam prefers segmentation over removal." },
      { t: "Segment it behind a firewall and monitor it", c: true, w: "Correct. Embedded and IoT devices that can't be patched should be isolated on their own segment with strict egress filtering and monitoring." },
      { t: "Install endpoint antivirus on the controller", c: false, w: "Embedded systems generally can't run endpoint agents — there's no general-purpose OS to install onto." },
    ],
  },
  {
    q: "A pair of firewalls is configured so that BOTH actively serve traffic, and either can absorb the other's load on failure. What is this called?",
    opts: [
      { t: "Active-passive high availability", c: false, w: "Active-passive keeps the second device on standby — it doesn't carry traffic until failover." },
      { t: "Active-active high availability", c: true, w: "Correct. Both devices serve traffic simultaneously; failure of one leaves the other handling everything." },
      { t: "Redundant configuration", c: false, w: "Redundancy means a spare exists; HA is the stronger guarantee that it's already running and ready to absorb load." },
      { t: "Load-balanced cluster", c: false, w: "A load balancer distributes traffic, but the firewall HA pair concept the exam tests is specifically active-active vs active-passive." },
    ],
  },
  {
    q: "Your organization buys cyber-security insurance to cover financial losses from a future ransomware incident. Which risk management strategy does this BEST illustrate?",
    opts: [
      { t: "Risk avoidance", c: false, w: "Avoidance would mean not doing the activity at all. Insurance acknowledges the risk and moves the cost." },
      { t: "Risk transference", c: true, w: "Correct. Insurance shifts the financial impact of a realized risk to a third party." },
      { t: "Risk acceptance", c: false, w: "Acceptance is doing nothing about the financial impact. Insurance is an explicit action to offload it." },
      { t: "Risk mitigation", c: false, w: "Mitigation reduces likelihood or impact through controls. Insurance doesn't reduce either — it pays you when the impact lands." },
    ],
  },
  {
    q: "An app in the cloud automatically spins up additional instances as traffic increases and tears them down when traffic drops. Which property does this BEST demonstrate?",
    opts: [
      { t: "Scalability", c: false, w: "Scalability means the system CAN grow. The automatic grow-and-shrink behavior is the more specific term." },
      { t: "Elasticity", c: true, w: "Correct. Elasticity is automatic, bidirectional scaling with demand — the cloud-native form of scalability." },
      { t: "Availability", c: false, w: "Availability is about being up for the right people. Auto-scaling supports availability but isn't the term for the behavior itself." },
      { t: "Resilience", c: false, w: "Resilience is about recovery from failure. The scenario describes responding to load, not failure." },
    ],
  },
  {
    q: "A SCADA network controls turbines at a power generation facility. What is the single MOST important security property the exam expects you to apply?",
    opts: [
      { t: "Endpoint antivirus on every controller", c: false, w: "Many SCADA endpoints can't run general-purpose AV. Not the primary control the exam tests for." },
      { t: "Strict segmentation from external networks", c: true, w: "Correct. ICS/SCADA networks must be heavily segmented from corporate and internet — compromise has physical-world consequences." },
      { t: "Daily patch cycles for all devices", c: false, w: "Many ICS devices can't be patched on a daily cadence; some can't be patched at all. Segmentation is the lead control." },
      { t: "Mandatory MFA for every field operator", c: false, w: "MFA helps but doesn't address the central concern — that the network shouldn't be reachable from outside in the first place." },
    ],
  },
];

// ---- Render ----
const root = dv.container;
root.empty();

const style = root.createEl("style");
style.textContent = `
  .quiz-card { border: 1px solid var(--background-modifier-border); padding: 18px 22px; margin-bottom: 22px; border-radius: 8px; background: var(--background-primary-alt); }
  .quiz-q { font-weight: 600; margin-bottom: 14px; line-height: 1.5; font-size: 1.0em; }
  .quiz-opt { display: flex; align-items: center; width: 100%; text-align: left; padding: 11px 14px; margin: 8px 0; border: 1px solid var(--background-modifier-border); background: var(--background-secondary); cursor: pointer; border-radius: 6px; color: var(--text-normal); font-size: 0.95em; line-height: 1.45; transition: all 120ms ease; }
  .quiz-opt:hover:not(:disabled) { background: var(--background-modifier-hover); }
  .quiz-opt.correct { background: rgba(63, 185, 80, 0.13); border-color: rgba(63, 185, 80, 0.45); color: var(--text-normal); }
  .quiz-opt.incorrect { background: rgba(248, 81, 73, 0.10); border-color: rgba(248, 81, 73, 0.35); color: var(--text-muted); opacity: 0.78; cursor: not-allowed; }
  .quiz-feedback { margin-top: 14px; padding: 11px 14px; border-radius: 6px; font-size: 0.9em; line-height: 1.5; }
  .quiz-feedback.correct { background: rgba(63, 185, 80, 0.06); border-left: 3px solid rgba(63, 185, 80, 0.6); color: var(--text-normal); }
  .quiz-feedback.incorrect { background: rgba(248, 81, 73, 0.06); border-left: 3px solid rgba(248, 81, 73, 0.55); color: var(--text-normal); }
  .quiz-progress { margin-bottom: 20px; padding: 10px 14px; background: var(--background-secondary); border-radius: 6px; font-size: 0.9em; color: var(--text-muted); }
`;

const progress = root.createDiv({ cls: "quiz-progress" });
let solvedCount = 0;
let wrongCount = 0;
const updateProgress = () => {
  progress.setText(`Progress: ${solvedCount} / ${questions.length} solved · ${wrongCount} wrong attempts`);
};
updateProgress();

questions.forEach((qData, i) => {
  const card = root.createDiv({ cls: "quiz-card" });
  card.createEl("div", { cls: "quiz-q", text: `Q${i + 1}. ${qData.q}` });
  let solved = false;
  const feedbackEl = card.createDiv({ cls: "quiz-feedback", attr: { style: "display:none" } });

  qData.opts.forEach((opt) => {
    const btn = card.createEl("button", { cls: "quiz-opt", text: opt.t });
    btn.onclick = () => {
      if (solved) return;
      if (opt.c) {
        btn.classList.add("correct");
        feedbackEl.style.display = "";
        feedbackEl.className = "quiz-feedback correct";
        feedbackEl.setText("✓ " + opt.w);
        solved = true;
        solvedCount++;
        updateProgress();
      } else {
        btn.classList.add("incorrect");
        btn.disabled = true;
        feedbackEl.style.display = "";
        feedbackEl.className = "quiz-feedback incorrect";
        feedbackEl.setText("✗ " + opt.w);
        wrongCount++;
        updateProgress();
      }
    };
  });
});
```

---

## Misses log

- *(none yet)*
