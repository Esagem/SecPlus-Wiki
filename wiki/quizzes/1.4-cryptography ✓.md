---
---

# 1.4 — Cryptographic Solutions Quiz

Eighteen questions on keys, hashing, signatures, certificates, obfuscation, and blockchain from [[1.4 ✓|Objective 1.4]].

```dataviewjs
const questions = [
  {
    q: "Why do real-world systems typically use BOTH symmetric and asymmetric encryption for a single TLS session?",
    opts: [
      { t: "Symmetric is faster but doesn't scale to many parties; asymmetric scales but is slow. Use asymmetric to exchange a session key, symmetric for the bulk traffic.", c: true, w: "Correct. Best of both: asymmetric handles the key-distribution problem, symmetric handles speed once the key is shared." },
      { t: "Asymmetric encryption is broken, so symmetric is used as backup", c: false, w: "Asymmetric isn't broken — it's just slow. Combining them is about performance, not security failure." },
      { t: "Symmetric is for encrypting; asymmetric is for hashing", c: false, w: "Asymmetric isn't a hashing algorithm. They're both encryption schemes." },
      { t: "Asymmetric is for data in transit only; symmetric is for data at rest only", c: false, w: "Both can do either. The split-of-roles in TLS is about handshake vs. bulk transport." },
    ],
  },
  {
    q: "Bob wants to send Alice an encrypted message. Whose public key does Bob use to encrypt the message?",
    opts: [
      { t: "Bob's own public key", c: false, w: "If Bob encrypted with his own public key, only Bob (with his private key) could decrypt — and the goal is for Alice to read it." },
      { t: "Alice's public key", c: true, w: "Correct. Encrypt with the recipient's public key so the recipient (and only the recipient, with her private key) can decrypt." },
      { t: "Alice's private key", c: false, w: "Only Alice has her private key. Bob doesn't have access — that's the whole point of asymmetric crypto." },
      { t: "A shared symmetric key the two agreed on out-of-band", c: false, w: "That's a valid alternative model, but the question specifies asymmetric encryption." },
    ],
  },
  {
    q: "An employee leaves the company. The company still needs access to files the employee encrypted with their personal key. What PKI concept ensures this is possible?",
    opts: [
      { t: "Key stretching", c: false, w: "Key stretching is a brute-force defense, not a recovery mechanism." },
      { t: "Key escrow", c: true, w: "Correct. A controlled third party stores a copy of the private key so it can be recovered when the original holder is unavailable." },
      { t: "Salting", c: false, w: "Salting is a hashing concept, not a key-recovery mechanism." },
      { t: "OCSP stapling", c: false, w: "OCSP stapling is about certificate revocation status, unrelated to recovering a missing private key." },
    ],
  },
  {
    q: "Which technology encrypts an ENTIRE Windows volume so the disk is unreadable without the proper key, even if removed from the machine?",
    opts: [
      { t: "EFS (Encrypting File System)", c: false, w: "EFS is file/folder-level encryption inside NTFS — it doesn't encrypt the whole volume." },
      { t: "BitLocker", c: true, w: "Correct. BitLocker is full-disk / volume-level encryption on Windows." },
      { t: "TPM", c: false, w: "A TPM is hardware that can STORE the encryption keys, but it's not the encryption mechanism itself." },
      { t: "EFS combined with a CSR", c: false, w: "A CSR is a certificate-request artifact, unrelated to disk encryption." },
    ],
  },
  {
    q: "A database has an `employees` table with columns: id, first_name, last_name, ssn. The team wants to encrypt the SSN column while keeping the others searchable in plaintext. What is this called?",
    opts: [
      { t: "Transparent database encryption", c: false, w: "Transparent encryption encrypts the entire database — every column. Too coarse for this requirement." },
      { t: "Full-disk encryption", c: false, w: "FDE is at the volume level, below the database layer. Wouldn't leave columns differentially readable." },
      { t: "Column-level encryption", c: true, w: "Correct. Encrypt only the sensitive column; the rest remain in plaintext for efficient search." },
      { t: "Record-level encryption", c: false, w: "Record-level encrypts a whole row. Here we want one column across all rows, not one row across all columns." },
    ],
  },
  {
    q: "AES and DES are both well-documented public algorithms. Why is publishing the algorithm's internals considered GOOD for security?",
    opts: [
      { t: "Public algorithms are weaker, which makes encryption faster", c: false, w: "Public algorithms aren't weaker. And speed isn't the reason for publishing them." },
      { t: "Public algorithms have been scrutinized by cryptographers worldwide; the key remains secret. Security depends on the key, not on hiding the math.", c: true, w: "Correct. Kerckhoffs's principle: the system should remain secure even if everything except the key is known. Public review hardens algorithms over time." },
      { t: "Public algorithms can run faster on consumer hardware", c: false, w: "Performance isn't why algorithms are made public. Open scrutiny is." },
      { t: "Publishing makes the algorithm easier to reverse-engineer for legitimate users", c: false, w: "Legitimate users implement the algorithm; they don't reverse-engineer it." },
    ],
  },
  {
    q: "In a Diffie-Hellman-style key exchange between Alice and Bob, what is actually TRANSMITTED across the network?",
    opts: [
      { t: "The final shared symmetric key, encrypted with a master key", c: false, w: "The shared key is NEVER transmitted in DH. That's the whole point." },
      { t: "Each party's PUBLIC values; the shared key is independently DERIVED on each side and never crosses the wire", c: true, w: "Correct. Both sides combine their own private value with the other side's public value to compute the same shared secret — without it ever being transmitted." },
      { t: "Each party's PRIVATE keys, encrypted with the other's public key", c: false, w: "Private keys never get transmitted under any circumstance." },
      { t: "A shared password agreed on out of band", c: false, w: "DH derives a shared secret in-band, not from a pre-shared password." },
    ],
  },
  {
    q: "A web browser establishes an HTTPS session. A symmetric key is generated specifically for this session and discarded when the session ends. What is this key called?",
    opts: [
      { t: "Root key", c: false, w: "Root key isn't a standard SY0-701 term in this context. Roots are about trust anchors, not session crypto." },
      { t: "Public key", c: false, w: "TLS does use public keys, but the per-session symmetric key isn't a public key — it's symmetric and ephemeral." },
      { t: "Session key (ephemeral)", c: true, w: "Correct. Session keys are temporary, used once, then discarded. Ephemeral by design — limits exposure if a key leaks." },
      { t: "Stretched key", c: false, w: "Stretching is a property of how a key is derived from a password, not what a one-session symmetric key is called." },
    ],
  },
  {
    q: "What is the KEY architectural difference between a TPM and an HSM?",
    opts: [
      { t: "A TPM is software; an HSM is hardware", c: false, w: "Both are hardware. The difference is scope, not implementation type." },
      { t: "A TPM serves a single device; an HSM serves many devices, typically clustered in a data center", c: true, w: "Correct. TPM = one local machine. HSM = centralized, high-performance, often redundant, supporting many systems." },
      { t: "A TPM uses asymmetric crypto only; an HSM uses symmetric only", c: false, w: "Both support both. The scope is what distinguishes them." },
      { t: "A TPM is cloud-only; an HSM is on-premises only", c: false, w: "Both are typically on-prem hardware. Cloud KMS / cloud HSM offerings exist, but the TPM/HSM distinction is per-device vs. shared." },
    ],
  },
  {
    q: "Which hashing algorithm is NO LONGER RECOMMENDED for security purposes due to documented collision vulnerabilities?",
    opts: [
      { t: "SHA-256", c: false, w: "SHA-256 is currently considered secure and is the SY0-701 default recommendation." },
      { t: "MD5", c: true, w: "Correct. MD5 has a documented collision (originally found in 1996) and is deprecated for security use — file integrity checks only, never password hashing." },
      { t: "SHA-512", c: false, w: "SHA-512 (part of SHA-2) is currently considered secure." },
      { t: "Bcrypt", c: false, w: "Bcrypt is a password-hashing function based on key stretching — still recommended." },
    ],
  },
  {
    q: "A password database is compromised. The attacker discovers that every password is hashed with a unique random value before being stored. What is this defense called, and what attack does it primarily defeat?",
    opts: [
      { t: "Key stretching — defeats brute force", c: false, w: "Key stretching is a related defense (adds compute cost per attempt), but the description — a unique random value per password — is salting. They defeat different things." },
      { t: "Salting — defeats rainbow-table lookups", c: true, w: "Correct. Salt is a unique random value added before hashing; it makes precomputed rainbow tables useless since the attacker would need a separate table per salt." },
      { t: "Tokenization — defeats replay attacks", c: false, w: "Tokenization is a transit/storage substitution, not a password-hashing technique." },
      { t: "Key escrow — defeats key loss", c: false, w: "Escrow is about recovery, not password hashing." },
    ],
  },
  {
    q: "Why are passwords HASHED rather than ENCRYPTED for storage?",
    opts: [
      { t: "Hashing is faster", c: false, w: "Modern password hashing is deliberately slow (key stretching). Speed isn't the reason." },
      { t: "Hashing is irreversible. Encryption is reversible — leak the key, leak every password. With a hash, even an admin can't recover the original.", c: true, w: "Correct. Irreversibility is the whole point. Login compares the hash of the entered password to the stored hash; never decrypts." },
      { t: "Encryption is illegal for passwords in most jurisdictions", c: false, w: "Not true — there's no general legal prohibition. The choice is architectural." },
      { t: "Hashing eliminates the need for the user to remember the password", c: false, w: "The user still needs to remember it. Hashing is server-side storage, not user-side simplification." },
    ],
  },
  {
    q: "Alice signs an email to Bob. Bob's email client verifies the signature successfully. What can Bob conclude?",
    opts: [
      { t: "Only that the email is encrypted", c: false, w: "Signing and encryption are separate operations. The signature says nothing about confidentiality." },
      { t: "The email body is unchanged AND it was sent by Alice — integrity plus non-repudiation", c: true, w: "Correct. A valid signature proves the hash of the received body matches what Alice signed AND that Alice's private key produced the signature." },
      { t: "Only that Alice's CA is trusted", c: false, w: "The CA's trust is necessary for verifying Alice's public key, but the verification result tells Bob about Alice and this specific message, not just the CA." },
      { t: "Only that the body is unchanged; nothing about who sent it", c: false, w: "A signature includes proof of origin too — that's what distinguishes a signature from a plain hash." },
    ],
  },
  {
    q: "An organization wants to install a single TLS certificate that will be valid for `www.example.com`, `mail.example.com`, `vpn.example.com`, and any future `*.example.com` subdomain. What is the BEST cert type?",
    opts: [
      { t: "Self-signed certificate", c: false, w: "Self-signed certs aren't trusted by external browsers by default, regardless of subject coverage." },
      { t: "Wildcard certificate", c: true, w: "Correct. `*.example.com` covers all single-level subdomains in one cert. Solves exactly this case." },
      { t: "OCSP-stapled certificate", c: false, w: "OCSP stapling is about revocation status, not subject coverage." },
      { t: "Three separate single-name certificates", c: false, w: "Possible but operationally painful and doesn't cover future subdomains automatically — exactly what the wildcard is designed to handle." },
    ],
  },
  {
    q: "An admin generates a key pair and bundles the public key plus identifying information into a file that will be sent to a CA for signing. What is this file called?",
    opts: [
      { t: "X.509 certificate", c: false, w: "An X.509 certificate is the SIGNED result returned by the CA, not the request sent to it." },
      { t: "Certificate Signing Request (CSR)", c: true, w: "Correct. The CSR carries the public key and subject info to the CA, which validates and signs it." },
      { t: "Certificate Revocation List entry", c: false, w: "A CRL entry lists a REVOKED cert. The opposite direction." },
      { t: "Key Management System token", c: false, w: "Not a standard term. KMSes manage keys, but the request artifact is called a CSR." },
    ],
  },
  {
    q: "What is the PRIMARY value a public Certificate Authority provides when you buy a certificate from them?",
    opts: [
      { t: "The mathematical strength of the digital signature itself", c: false, w: "The CA's signature is cryptographic but the math is no stronger than anyone else's. The value is elsewhere." },
      { t: "The validation that you actually own/control the domain or identity being certified", c: true, w: "Correct. You're paying for the CA's verification process. The signature merely attests that this verification was performed by a CA browsers already trust." },
      { t: "Permission to use the public key", c: false, w: "Public keys don't require permission — you generate your own." },
      { t: "Discounted hosting", c: false, w: "Not a CA function." },
    ],
  },
  {
    q: "A web server includes a recent, CA-signed certificate-status response directly in its TLS handshake, so the connecting browser does not need to make a separate lookup. What is this called?",
    opts: [
      { t: "CRL (Certificate Revocation List)", c: false, w: "A CRL is a downloaded list of revoked certs — the heavyweight approach. The described scenario avoids that download." },
      { t: "OCSP stapling", c: true, w: "Correct. The server periodically queries the CA for an OCSP response and 'staples' that signed response into every TLS handshake. No client-side OCSP traffic." },
      { t: "Wildcard certificate", c: false, w: "Wildcards are about which names a cert covers, not about revocation status." },
      { t: "Heartbleed protection", c: false, w: "Heartbleed was a specific OpenSSL flaw. OCSP stapling is a general mechanism, not Heartbleed-specific." },
    ],
  },
  {
    q: "A customer pays at a store using their phone. The merchant never sees the actual credit-card number — instead they receive a one-time string that maps back to the card only via a trusted lookup service, and replaying the same string is rejected. What is this technique?",
    opts: [
      { t: "Tokenization", c: true, w: "Correct. Sensitive data is replaced by a meaningless token with no mathematical relationship to the original. Resolution happens only at a trusted service. One-time tokens defeat replay." },
      { t: "Data masking", c: false, w: "Data masking is for DISPLAY (asterisks on a receipt), not for substituting data in transit." },
      { t: "Steganography", c: false, w: "Steganography hides data in another medium. Tokenization substitutes data outright with an unrelated token." },
      { t: "Symmetric encryption", c: false, w: "Encryption produces ciphertext mathematically related to the plaintext. A token has NO mathematical relationship to the original — that's its defining property." },
    ],
  },
];

// ---- Setup (load shared scaffold) ----
const baseName = "1.4-cryptography";
try {
  const _scaffoldFile = app.vault.getAbstractFileByPath("wiki/quizzes/_quiz-scaffold.md");
  if (!_scaffoldFile) throw new Error("Scaffold not found: wiki/quizzes/_quiz-scaffold.md");
  const _src = await app.vault.read(_scaffoldFile);
  const _m = _src.match(/```javascript[ \t]*\r?\n([\s\S]*?)\r?\n[ \t]*```/);
  if (!_m) throw new Error("Could not extract scaffold JS code block");
  new Function("questions", "baseName", "dv", "app", "window", _m[1])(questions, baseName, dv, app, window);
} catch (e) {
  console.error("[quiz] scaffold load failed:", e);
  const errDiv = dv.container.createEl("div");
  errDiv.style.cssText = "color: var(--text-error); padding: 12px; border: 1px solid var(--text-error); border-radius: 6px;";
  errDiv.setText("Quiz scaffold failed to load: " + (e?.message || e));
}
```

---

# 1.4 — Cryptography Quiz

> 🛑 **Quiz content lost.** This file was overwritten on 2026-05-25 by a `wiki_write` refactor pass that dropped the questions array. See [[synthesis/aar-2026-05-25|AAR 2026-05-25]] for the postmortem. **Restore the question bank from git history or local Obsidian trash before quizzing on 1.4.**

---

_Attempt history is appended to [[wiki/sessions/quiz-logs/1.4-cryptography.log|wiki/sessions/quiz-logs/1.4-cryptography.log]] each time you complete the quiz. Hit Reset to clear progress and start over (the log is kept)._
