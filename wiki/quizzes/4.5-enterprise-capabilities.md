---
title: 4.5 — Enterprise Capabilities Quiz
category: quiz
tags: [quiz, firewalls, dlp, edr, email-security, web-filter, 4.5]
status: active
confidence: high
created: 2026-05-21
updated: 2026-05-21
covers: [4.5]
---

# 4.5 — Enterprise Capabilities Quiz

Fifteen questions on firewalls, web filtering, OS controls, secure protocols, email security, data monitoring, and endpoint security from [[objectives/4.5|Objective 4.5]].

```dataviewjs
const questions = [
  {
    q: "A firewall rule list has an implicit deny at the bottom. Traffic enters that doesn't match any explicit allow rule. What happens?",
    opts: [
      { t: "It is forwarded with a warning logged", c: false, w: "Implicit deny doesn't log a warning and forward — it drops." },
      { t: "It is dropped", c: true, w: "Correct. Implicit deny is the catch-all: anything that doesn't match an explicit allow rule is silently dropped." },
      { t: "It is forwarded to the destination", c: false, w: "Implicit deny is the opposite of implicit allow. Traffic without a match is dropped, not forwarded." },
      { t: "The firewall reboots", c: false, w: "Implicit deny is a routing decision, not a fault." },
    ],
  },
  {
    q: "Which is the BEST distinction between SPF, DKIM, and DMARC?",
    opts: [
      { t: "They are three names for the same protocol", c: false, w: "Three distinct mechanisms with separate jobs." },
      { t: "SPF lists authorized sending servers, DKIM signs individual messages, DMARC tells receivers what to do on SPF/DKIM failure and where to send reports", c: true, w: "Correct. SPF = who can send. DKIM = per-message signature. DMARC = policy (accept/quarantine/reject) on failure + reporting destination." },
      { t: "SPF and DKIM are sending tools; DMARC is a receiving tool", c: false, w: "All three are DNS records published by the sending domain; all three are checked by the receiving server." },
      { t: "DMARC is required; SPF and DKIM are optional", c: false, w: "DMARC builds on SPF/DKIM — they're prerequisites, not alternatives." },
    ],
  },
  {
    q: "A user wants to harden a Linux server by enforcing centrally-defined access rules that users cannot override. Which feature provides MANDATORY access control?",
    opts: [
      { t: "Default Linux file permissions (chmod, chown)", c: false, w: "Default Linux is DAC — the user controls their own file permissions. Not mandatory." },
      { t: "SELinux (Security-Enhanced Linux)", c: true, w: "Correct. SELinux adds MAC on top of Linux's default DAC. The administrator defines policy; users can't override it. Used in highly secure environments." },
      { t: "Active Directory", c: false, w: "AD is a Windows-centric directory; not the mandatory access control mechanism for Linux." },
      { t: "Group Policy", c: false, w: "Group Policy is Windows. Linux equivalent for MAC is SELinux (or AppArmor)." },
    ],
  },
  {
    q: "Which scenario is BEST handled by DNS filtering rather than a URL filter on the NGFW?",
    opts: [
      { t: "A user visits a website via the corporate web browser", c: false, w: "Either tool can handle this; URL filtering on the NGFW would do it." },
      { t: "Malware on a compromised laptop tries to resolve its command-and-control server's hostname", c: true, w: "Correct. DNS filtering catches anything that does a DNS lookup — including malware reaching for C2. It works for non-browser traffic too." },
      { t: "A user attempts to upload data to a SaaS service", c: false, w: "DLP, not DNS filtering. DNS filter doesn't see file content." },
      { t: "A user logs into a VPN", c: false, w: "VPN auth isn't the typical DNS filtering use case." },
    ],
  },
  {
    q: "An endpoint DLP agent watches for sensitive data in which data state?",
    opts: [
      { t: "Data in motion", c: false, w: "Data in motion is a network-DLP placement, not endpoint." },
      { t: "Data in use", c: true, w: "Correct. The endpoint's active memory / accessed-by-user state is 'data in use.' Endpoint DLP runs as software on the workstation watching exactly this." },
      { t: "Data at rest", c: false, w: "Data at rest is stored on disk; DLP for that runs on the file server or storage system, not the endpoint per se." },
      { t: "Data in escrow", c: false, w: "Not a DLP state — escrow is a key-management term." },
    ],
  },
  {
    q: "Which capability BEST distinguishes EDR from traditional antivirus?",
    opts: [
      { t: "EDR uses signatures; AV does not", c: false, w: "Both use signatures. EDR adds capabilities on top." },
      { t: "EDR adds behavioral analysis, automated response (isolation, rollback), and root-cause analysis", c: true, w: "Correct. EDR layers behavioral analytics, ML, automated response (quarantine the process, isolate the host, roll back), and post-incident root-cause analysis on top of signature scanning." },
      { t: "EDR is free; AV is paid", c: false, w: "Both have commercial and free variants. Cost isn't the distinguishing feature." },
      { t: "EDR runs only in the cloud", c: false, w: "EDR has an agent on the endpoint; management/correlation may live in the cloud, but enforcement is local." },
    ],
  },
  {
    q: "XDR adds what to an EDR architecture?",
    opts: [
      { t: "Cross-source correlation across many endpoints, networks, and identity systems, plus user-behavior analytics", c: true, w: "Correct. XDR scales EDR by ingesting many data sources (endpoints, network telemetry, identity) and correlating across them — single-endpoint blind spots disappear." },
      { t: "Stronger encryption of endpoint files", c: false, w: "Not the XDR value-add." },
      { t: "Replacement for the firewall", c: false, w: "XDR augments visibility and response; doesn't replace firewalling." },
      { t: "Manual investigation only — no automation", c: false, w: "Backwards — XDR adds automation, not removes it." },
    ],
  },
  {
    q: "An NGFW needs to distinguish 'reading Twitter' from 'posting to Twitter' even though both use TCP 443. Which capability makes this possible?",
    opts: [
      { t: "ICMP filtering", c: false, w: "ICMP filtering is unrelated to L7 app identification." },
      { t: "L7 / application-layer deep packet inspection", c: true, w: "Correct. NGFWs identify the application within encrypted/standard ports by deep packet inspection — they can tell apart different actions within the same app." },
      { t: "MAC address filtering", c: false, w: "MAC filtering operates at L2 and doesn't distinguish app actions." },
      { t: "URL category lookup only", c: false, w: "URL categorization alone doesn't separate viewing from posting to the same site." },
    ],
  },
  {
    q: "A company's mail-gateway sees an inbound email that fails BOTH SPF and DKIM checks. The sending domain's DMARC policy is 'p=quarantine'. What does the receiver do?",
    opts: [
      { t: "Deliver to the inbox normally", c: false, w: "DMARC quarantine instructs the receiver to NOT deliver to inbox." },
      { t: "Send to the spam / junk folder", c: true, w: "Correct. p=quarantine = isolate (typically to spam). p=reject = drop. p=none = accept and just report." },
      { t: "Bounce back to the sender", c: false, w: "Bounces aren't part of DMARC. Reject is a drop, not a bounce." },
      { t: "Forward to the DMARC reporting address", c: false, w: "Reports are separate aggregated data sent back to the sender domain owner — not the original message." },
    ],
  },
  {
    q: "Which is the BEST description of a forward proxy's role?",
    opts: [
      { t: "Defends internal web servers from inbound internet traffic", c: false, w: "That's a reverse proxy. Forward and reverse defend different sides." },
      { t: "Sits between internal clients and the internet, making outbound requests on their behalf; can cache, filter, and enforce access control", c: true, w: "Correct. Forward proxy = internal users go OUT through it. Reverse proxy = internet users come IN through it to internal servers." },
      { t: "Encrypts traffic between two branch offices", c: false, w: "That's a site-to-site VPN." },
      { t: "Stores all DNS records for the domain", c: false, w: "That's a DNS server, not a proxy." },
    ],
  },
  {
    q: "Tripwire on Linux and System File Checker (sfc) on Windows are examples of what kind of monitoring?",
    opts: [
      { t: "Data Loss Prevention (DLP)", c: false, w: "DLP looks for sensitive data content; FIM watches file integrity." },
      { t: "File Integrity Monitoring (FIM)", c: true, w: "Correct. Both tools watch critical files (binaries, libraries, OS files) for unexpected modification. sfc is on-demand; Tripwire offers continuous monitoring." },
      { t: "Vulnerability scanning", c: false, w: "Vulnerability scanners check version+config for known CVEs, not file integrity." },
      { t: "Network access control", c: false, w: "NAC controls device admission; not file integrity." },
    ],
  },
  {
    q: "A user must connect to many corporate services from a laptop on a hotel Wi-Fi. The applications themselves don't all support TLS. What is the BEST way to secure all this traffic?",
    opts: [
      { t: "Patch each application individually to add TLS support", c: false, w: "Often impossible (apps may not have TLS options), slow, and doesn't address the wireless network risk." },
      { t: "Connect via a VPN (encrypted tunnel from device to concentrator)", c: true, w: "Correct. A VPN encrypts everything inside the tunnel regardless of whether the underlying app uses encryption — the right tool when you can't make every app secure on its own." },
      { t: "Switch to HTTP from HTTPS", c: false, w: "HTTP is the unencrypted version — backwards." },
      { t: "Disable all network adapters", c: false, w: "No connectivity means no work — not a security solution." },
    ],
  },
  {
    q: "A device must comply with corporate security policy before being allowed onto the network. The check happens during login and removes itself after. Which kind of agent is this?",
    opts: [
      { t: "Persistent agent", c: false, w: "Persistent agents are installed and always running — not what this describes." },
      { t: "Dissolvable agent", c: true, w: "Correct. Dissolvable agents execute at login/connection, do the posture check, and remove themselves afterward — no permanent install." },
      { t: "EDR agent", c: false, w: "EDR is a different category (continuous threat detection); not specifically a dissolvable posture-check agent." },
      { t: "Headless agent", c: false, w: "Not the standard term for this concept." },
    ],
  },
  {
    q: "Capturing traffic on TCP port 443 shows the bytes are encrypted. What does this confirm?",
    opts: [
      { t: "The connection is using HTTPS specifically", c: false, w: "Port 443 *usually* implies HTTPS, but the port number alone doesn't prove what protocol is in use — only that some encrypted protocol is." },
      { t: "The traffic is encrypted, but the port number alone doesn't prove which specific protocol is in use", c: true, w: "Correct. A server could run any protocol on port 443. The packet capture confirms encryption is happening; verifying the protocol means inspecting the handshake or server config." },
      { t: "The user is authorized", c: false, w: "Encryption doesn't authenticate or authorize the user." },
      { t: "The DNS resolver is functioning", c: false, w: "Unrelated to packet capture of an established connection." },
    ],
  },
  {
    q: "An organization installs DLP software on its file servers to scan stored documents for Social Security numbers. Which data state does this protect?",
    opts: [
      { t: "Data in motion", c: false, w: "Data in motion is on the network. Files at rest on a server is a different state." },
      { t: "Data in use", c: false, w: "Data in use is in active memory of a workstation; not files-at-rest on a server." },
      { t: "Data at rest", c: true, w: "Correct. Files stored on disk are 'data at rest.' DLP for this state runs as software on the file/database server scanning the filesystem." },
      { t: "Data in transit only at OSI L3", c: false, w: "OSI layer isn't relevant; the DLP state model uses the in-use / in-motion / at-rest taxonomy." },
    ],
  },
];

// ---- Render ----
const root = dv.container;
root.empty();

const style = root.createEl("style");
style.textContent = `
  .quiz-card { border: 1px solid var(--background-modifier-border); padding: 18px 22px; margin-bottom: 22px; border-radius: 8px; background: var(--background-primary-alt); }
  .quiz-q { font-weight: 600; margin-bottom: 14px; line-height: 1.5; font-size: 1.0em; }
  .quiz-opt { display: flex; align-items: center; width: 100%; text-align: left; padding: 11px 14px; margin: 8px 0; border: 1px solid var(--background-modifier-border); background: var(--background-secondary); cursor: pointer; border-radius: 6px; color: var(--text-normal); font-size: 0.95em; line-height: 1.45; transition: all 120ms ease; }
  .quiz-opt:hover:not(:disabled) { background: var(--background-modifier-hover); }
  .quiz-opt.correct { background: rgba(63, 185, 80, 0.13); border-color: rgba(63, 185, 80, 0.45); color: var(--text-normal); }
  .quiz-opt.incorrect { background: rgba(248, 81, 73, 0.10); border-color: rgba(248, 81, 73, 0.35); color: var(--text-muted); opacity: 0.78; cursor: not-allowed; }
  .quiz-feedback { margin-top: 14px; padding: 11px 14px; border-radius: 6px; font-size: 0.9em; line-height: 1.5; }
  .quiz-feedback.correct { background: rgba(63, 185, 80, 0.06); border-left: 3px solid rgba(63, 185, 80, 0.6); color: var(--text-normal); }
  .quiz-feedback.incorrect { background: rgba(248, 81, 73, 0.06); border-left: 3px solid rgba(248, 81, 73, 0.55); color: var(--text-normal); }
  .quiz-progress { margin-bottom: 20px; padding: 10px 14px; background: var(--background-secondary); border-radius: 6px; font-size: 0.9em; color: var(--text-muted); }
`;

const progress = root.createDiv({ cls: "quiz-progress" });
let solvedCount = 0;
let wrongCount = 0;
const updateProgress = () => {
  progress.setText(`Progress: ${solvedCount} / ${questions.length} solved · ${wrongCount} wrong attempts`);
};
updateProgress();

questions.forEach((qData, i) => {
  const card = root.createDiv({ cls: "quiz-card" });
  card.createEl("div", { cls: "quiz-q", text: `Q${i + 1}. ${qData.q}` });
  let solved = false;
  const feedbackEl = card.createDiv({ cls: "quiz-feedback", attr: { style: "display:none" } });

  qData.opts.forEach((opt) => {
    const btn = card.createEl("button", { cls: "quiz-opt", text: opt.t });
    btn.onclick = () => {
      if (solved) return;
      if (opt.c) {
        btn.classList.add("correct");
        feedbackEl.style.display = "";
        feedbackEl.className = "quiz-feedback correct";
        feedbackEl.setText("✓ " + opt.w);
        solved = true;
        solvedCount++;
        updateProgress();
      } else {
        btn.classList.add("incorrect");
        btn.disabled = true;
        feedbackEl.style.display = "";
        feedbackEl.className = "quiz-feedback incorrect";
        feedbackEl.setText("✗ " + opt.w);
        wrongCount++;
        updateProgress();
      }
    };
  });
});
```

---

## Misses log

- *(none yet)*
